schema {
    query: Query
    mutation: Mutation
}


type User {
    id: ID
    name: String
    age: Int
}

extend type Query {
  user(id:String): User!
}
extend type Mutation { 
  addUser(id: String!, name:String!, age:Int!): User!
}